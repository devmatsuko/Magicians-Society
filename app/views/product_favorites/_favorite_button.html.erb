<!-- カレントユーザがいいねをしているか判定する -->
<% if user_signed_in? && product.favorited_by?(current_user) %>
	<%= link_to product_product_favorites_path(product), method: :delete, remote: true do %>
		<div class="btn btn-secondary">
			<i class="fas fa-heart" aria-hidden="true" style="color: red;"></i>
			<span><%= product.product_favorites.count %>　いいね! </span>
		</div>
	<% end %>
<% else %>
	<%= link_to product_product_favorites_path(product), method: :post, remote: true do %>
	<div class="btn btn-secondary">
		<i class="far fa-heart" aria-hidden="true"></i>
		<span><%= product.product_favorites.count %>　いいね! </span>
	</div>
	<% end %>
<% end %>
