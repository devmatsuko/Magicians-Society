$('#chat-area').html("<%= j(render partial: 'messages/message', locals: { messages: @messages}) %>");
$('#message_content').val("");
<!-- スクロールを一番下にする -->
var obj = document.getElementById("scroll");
<!-- スクロール要素があった場合のみ実行 -->
if(obj){
  obj.scrollTop = obj.scrollHeight;
}